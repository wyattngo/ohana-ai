# SMOKE — spec 11 phase S0 (`shops` + `shop_profile` + validate JSONB lúc ghi)
- diff_sha256: 03284d3acd9f3b52f4aafb5154668bbcf659c58110debc928575760b6a47ac8b
- stamped_at: 2026-07-20T22:02

SMOKED_BY: Claude (Opus 4.8) — script chạy thật trên Postgres thật, output dán nguyên văn
VERDICT: PASS

**Ngày:** 2026-07-20 · **Môi trường:** Postgres THẬT (`drnickv4-db-1`, pgvector pg16) @ `127.0.0.1:5432/ohana`

Test đo `fresh_db` (schema từ `Base.metadata.create_all`). Smoke này đo cái test không đo: **chuỗi migration thật** + **luồng ghi/đọc/ráp-prompt trên schema do Alembic dựng**.

---

## 1. Chuỗi migration `0 → head → down → up`

```
$ alembic upgrade head        → head = 0007
$ alembic downgrade -1        → 0007 → 0006
$ alembic upgrade head        → 0006 → 0007
```

**OBSERVED:** cả ba exit 0. Schema thật đọc từ Postgres:

```
alembic head: 0007
shops:        [('id','text','NO'), ('name','text','NO'), ('status','text','NO'), ('created_at','timestamptz','NO')]
shop_profile: [('shop_id','text','NO'), ('persona_md','text','NO'), ('knowledge','jsonb','NO'),
               ('published_at','timestamptz','YES'), ('updated_at','timestamptz','NO')]
constraint:   [('ck_shop_profile_persona_len','c'), ('shop_profile_pkey','p'), ('shop_profile_shop_id_fkey','f')]
```

Đủ ba: CHECK (cap persona) + PK (một profile mỗi shop) + FK (shop phải tồn tại). `published_at` là cột **duy nhất** nullable — đúng PRE-1102.

## 2. Luồng thật: ghi → đọc → ráp prompt

```
published_at: None (None = chưa phát hành)
knowledge đã chuẩn hoá: {'size_chart': [{'size': 'M', 'height_max_cm': 165, 'height_min_cm': 155,
                                          'weight_max_kg': 55, 'weight_min_kg': 45}],
                         'shipping_zones': [{'zone': 'Q7', 'fee_vnd': 25000, 'eta_days': 2}]}
---- PROMPT ráp ra ----
Bạn là trợ lý trả lời tin nhắn khách hàng của shop "Shop Áo Thun 24h".

Shop bán áo thun nữ. Xưng 'shop', gọi khách 'bạn'. Thân thiện, không dùng emoji quá 1 cái.

Luôn xưng danh là shop. TUYỆT ĐỐI không nhắc tới nền tảng, công cụ, hay nhà cung cấp AI nào
đứng sau. Không nói mình là AI, bot, hay trợ lý ảo.
---- độ dài: 307 ký tự (cap persona 2000) ----
shop KHÁC đọc: None
field lạ bị chặn: ValidationError — size_charts
```

**OBSERVED:**
- `knowledge` được **ghi lại dạng đã chuẩn hoá** (`model_dump()`), không phải dict thô của caller.
- `shop_khac` đọc cùng profile ⇒ **`None`**, không raise — không rò rỉ sự tồn tại.
- Field lạ `size_charts` (thừa `s`) bị **TỪ CHỐI lúc GHI**, không bị nuốt im lặng. Đây là ca thật: nếu bị nuốt, seller thấy "lưu thành công" rồi `lookup_size` trả `not_found` mãi mãi mà không gì giải thích.
- Prompt ráp ra 307 ký tự cho một persona thật ⇒ cap 2000 rộng rãi ở ca điển hình.

## 3. Gate

```
pytest tests/ -m 'not live'  → 165 passed, 6 deselected   (S0: 154 → +11)
mypy … api auth              → Success: 51 source files
ruff check . --no-cache      → All checks passed!
ruff format --check          → 82 files already formatted
```

RED trước impl: `ModuleNotFoundError: No module named 'agent.persona'` — API chưa tồn tại, cùng dạng RED của 10-H0/H2.

## 4. Áp dụng bài học `10-H0.md §4` — và nó có tác dụng

Reset schema lần này drop **TỪNG BẢNG** (`drop table ... cascade` trong vòng lặp) thay vì `drop schema public cascade`. Kết quả: extension `vector` **sống sót**, không phải xin superuser `drnick` tạo lại như lần trước.

```
drop 9 bảng (giữ extension vector)
vector còn: ('vector',)
```

Bẫy pytest-làm-bẩn-schema-alembic vẫn nguyên: phải reset TRƯỚC mỗi lần verify chuỗi migration, vì `fresh_db` dựng schema từ `Base.metadata` mà không đụng `alembic_version`.

## 5. Chưa đo (cố ý)

- **`lookup_size` / `lookup_shipping` chưa tồn tại** — S2. Smoke này chứng minh `knowledge` được lưu ĐÚNG SHAPE, không chứng minh có ai tra cứu được.
- **Onboard + JWT thật** — S1. `shops` hiện chỉ tạo được bằng tay/script; chưa có endpoint, và `auth/identity.py` vẫn tin `shop_id` chỉ vì chữ ký hợp lệ.
- **AI không lộ danh tính** — prompt CÓ chỉ dẫn, nhưng chỉ dẫn không phải bằng chứng. Phải đo trên OUTPUT thật với `Drafter` + LLM (`GD0-DRAFTER` acceptance). Đừng đọc §2 rộng hơn thế.
