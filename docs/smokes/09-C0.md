# SMOKE — phase C0 (ohana-ai)

- generated: 2026-07-20T08:35
- diff_sha256: 9c77a82c75cc259c6e67ed9bf7956bb42da34ca5ba240510b86d436d25dbe8fc
- stamped_at: 2026-07-20T08:44

## Mặt runtime của C0

Migration + ràng buộc trên Postgres thật. Test đo môi trường TEST (schema dựng bằng
`Base.metadata.create_all`, KHÔNG qua Alembic — xem docstring `conftest.fresh_db`); smoke này
đo đường Alembic thật và hỏi thẳng catalog.

## Checklist

### [x] R1 — Migration up→down→up trên Postgres thật

OBSERVED:
```
-- head hiện tại      : 0005 (head)
-- sau downgrade 0004 : 0004
-- sau upgrade head   : 0005 (head)
```
Reversible **thật** cả hai chiều, khác hẳn `0004` của spec 08 (cái đó xoá dữ liệu ở cả up lẫn
down). Ở đây không dòng dữ liệu nào bị đụng.

### [x] R2 — Ràng buộc tồn tại và MANG ĐÚNG CỜ

OBSERVED (`pg_constraint` ⋈ `pg_index`):
```
uq_conversations_shop_cus_chan_thread
   cols = ['shop_id', 'customer_id', 'channel', 'external_thread_id']
   NULLS NOT DISTINCT = True
uq_conversations_shop_id
   cols = ['shop_id', 'id']
   NULLS NOT DISTINCT = False
```
Cờ đọc từ `pg_index.indnullsnotdistinct` — **không phải** `pg_constraint`, nơi bản đầu của
test đi tìm và chết bằng `UndefinedColumn`. Constraint cũ `uq_conversations_shop_id` còn
nguyên, không bị migration đụng.

### [x] R3 — Cờ đó CÓ CHẶN THẬT không (SQL thô, không qua ORM)

OBSERVED:
```
thread_id=NULL lần 1                       CHẤP NHẬN
thread_id=NULL lần 2 (trùng)               TỪ CHỐI — IntegrityError
thread_id='thread_A'                       CHẤP NHẬN
thread_id='thread_B'                       CHẤP NHẬN
thread_id='thread_A' lần 2 (trùng)         TỪ CHỐI — IntegrityError

→ còn lại 3 conversation (kỳ vọng 3: NULL + thread_A + thread_B)
```

Hai dòng đắt nhất là **NULL lần 2 bị TỪ CHỐI** và **thread_B được CHẤP NHẬN**:

1. NULL-lần-2 bị chặn ⇒ `NULLS NOT DISTINCT` có tác dụng thật. Không có cờ này, một UNIQUE
   thường sẽ CHO QUA cả hai — constraint có mặt mà race còn nguyên. Đây là ca phổ biến nhất
   hôm nay vì Zalo không phải lúc nào cũng gửi `thread_id`.
2. thread_B qua được ⇒ phương án **B** đúng như thiết kế: hai thread khác nhau vẫn tách được.
   Phương án A (bỏ `external_thread_id` khỏi khoá) sẽ TỪ CHỐI dòng này và âm thầm gộp hai
   mạch hội thoại làm một.

Kiểm ở tầng SQL thô, không qua ORM — bảo vệ phải đúng kể cả khi caller viết sai.

### [x] R3b — Test CÓ RĂNG không (mutation, do review yêu cầu)

Review vòng 1 trả **NEEDS_REVIEW** với một nhận xét đúng: test (c) chứng minh constraint chặn
*plain INSERT*, nhưng code thật dùng `on_conflict_do_nothing` + re-select — giữa hai thứ đó có
khoảng trống. Cụ thể: khi B đụng row A chưa commit, B bị CHẶN; A commit; B no-op — lúc đó
re-select của B có thấy row không, hay `scalar_one()` ném `NoResultFound` và webhook trả 500?
Trước đó câu trả lời dựa trên tài liệu Postgres, không phải phép đo.

Đã thêm `test_upsert_shape_survives_real_blocking_race` — dựng đúng thứ tự đó tường minh.
Rồi kiểm chính test ấy bằng **mutation**:

```
mutation 1 — bỏ hẳn constraint khỏi model:
  ProgrammingError: constraint "uq_conversations_shop_cus_chan_thread" does not exist
  → đỏ, nhưng đỏ vì "thiếu constraint", CHƯA chứng minh assertion chính có răng

mutation 2 — GIỮ constraint, chỉ tắt cờ (nulls_not_distinct=False):
  AssertionError: B KHÔNG bị chặn — nghĩa là không có xung đột nào xảy ra,
                  tức constraint không bao phủ ca này. Race vẫn còn nguyên.
  → ĐÚNG assertion đó nổ, với ĐÚNG thông điệp đó. 4/8 test đỏ.
```

Mutation 2 là phép thử có giá trị: nó dựng lại chính xác kịch bản "constraint trông như đã vá
mà không vá gì". Không có nó thì `NULLS NOT DISTINCT` chỉ là một dòng người ta tin nhau.

### [–] R4 — Rò tenant
**N/A với đường ghi.** Constraint có `shop_id` ở vị trí đầu khoá nên không nới lỏng ranh giới
tenant. Có test riêng canh (`test_cross_shop_same_customer_stays_separate`): cùng một người
nhắn 2 shop ⇒ 2 customer + 2 conversation, vẫn xanh.

### [x] R0 — Có gì bất ngờ không?

```
1. Test race đầu tiên tôi viết ĐÃ XANH TRƯỚC KHI CÓ CONSTRAINT
```
Bản đầu dùng `asyncio.gather` trên hai `resolve_conversation()`. Nó xanh ở bước RED — nghĩa là
nó **không tái hiện được race**: hai transaction không đan xen, mỗi cái chạy trọn trước cái
kia. Một test xanh trước bản vá không chứng minh gì về bản vá. Đã thay bằng test viết thẳng
thứ tự SELECT-A → SELECT-B → INSERT-A → INSERT-B (tất định), và test `gather` bị hạ vai trò
với docstring ghi rõ đừng đọc nó như bằng chứng race.

Đây là lần thứ tư trong sprint này tôi tự tạo ra một phép đo xanh-mà-rỗng. Ba lần trước ở spec
08. Cùng một hình dạng với `.ruff_cache`.

```
2. Thêm 0005 làm ĐỎ 2 test của spec 08 — mà không dòng nào của spec 08 sai
```
`test_embedding_dim.py` dùng `alembic downgrade -1`, nghĩa là "lùi một bước từ head". Nó chỉ
trỏ đúng `0004` chừng nào `0004` còn là head. Thêm `0005` là nó trỏ nhầm. Test nói về migration
`0004` thì phải GỌI TÊN `0004`, không mô tả vị trí tương đối. Đã sửa sang target cố định
`0003`/`0004`. Bẫy này sẽ nổ với BẤT KỲ migration nào thêm sau — nó chỉ tình cờ chưa nổ.

```
3. Query catalog sai bảng
```
`NULLS NOT DISTINCT` sống trên `pg_index.indnullsnotdistinct`, không phải `pg_constraint`
(bảng đó không có cột nào như vậy). Bước RED bắt được vì test chết bằng `UndefinedColumn` chứ
không phải bằng assertion — dấu hiệu để phân biệt "đỏ vì đúng lý do" với "đỏ vì test hỏng".

---
SMOKED_BY: Claude (Opus 4.8), chạy tay 2026-07-20, Postgres 16.14 thật tại DATABASE_URL của `.env`
VERDICT: PASS
