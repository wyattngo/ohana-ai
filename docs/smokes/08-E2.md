# SMOKE — phase E2 (ohana-ai)

- generated: 2026-07-19T23:14
- diff_sha256: 1cd0c0da72331ec7ad32b993bc0c6b5a8d7313538896970ce7afe67e6e224fd9
- stamped_at: 2026-07-19T23:14

## Mặt runtime của E2

**Phase này CHÍNH LÀ smoke.** E2 không thêm code chạy production — nó chạy live acceptance và
đóng ISSUE-016. Bằng chứng phải là output live thật, không phải "4 passed".

## Checklist

### [x] R1 — `pytest -m live` với key thật, e5 thật, Postgres thật

OBSERVED:
```
$ TOGETHER_API_KEY=... pytest tests/test_wiki_rag_live.py -m live -q
....                                                                     [100%]
```
4 test = 3 ca thứ hạng (parametrize) + 1 ca dim. Nhưng "4 passed" chưa phải bằng chứng —
xem R2.

### [x] R2 — THỨ HẠNG thật, không phải "có trả về gì đó"

OBSERVED (probe in ra xếp hạng thật, cùng corpus/embedder với test):
```
model        : intfloat/multilingual-e5-large-instruct
EMBED_DIM    : 1024
ingest       : 3 chunk trong 1.02s

Q: "phí ship nội thành bao nhiêu tiền"  (0.36s)
   #0 'Phí vận chuyển: nội thành 25.000đ, ngoại thành 35.000đ. Đơn từ 5' ← ĐẦU
   #1 'Giờ mở cửa: cửa hàng hoạt động từ 8h đến 21h các ngày trong tuần'
   #2 'Chính sách đổi trả: khách được đổi hoặc trả hàng trong vòng 7 ng'

Q: "đổi hàng được không"  (0.32s)
   #0 'Chính sách đổi trả: khách được đổi hoặc trả hàng trong vòng 7 ng' ← ĐẦU
   #1 'Phí vận chuyển: nội thành 25.000đ, ngoại thành 35.000đ. Đơn từ 5'
   #2 'Giờ mở cửa: cửa hàng hoạt động từ 8h đến 21h các ngày trong tuần'

Q: "mấy giờ đóng cửa chủ nhật"  (0.34s)
   #0 'Giờ mở cửa: cửa hàng hoạt động từ 8h đến 21h các ngày trong tuần' ← ĐẦU
   #1 'Chính sách đổi trả: khách được đổi hoặc trả hàng trong vòng 7 ng'
   #2 'Phí vận chuyển: nội thành 25.000đ, ngoại thành 35.000đ. Đơn từ 5'
```

**3/3 truy vấn kéo đúng chunk lên #0, và mỗi câu kéo một chunk KHÁC.** Vế sau mới là phần
đáng giá: một truy vấn duy nhất có thể xanh vì một chunk tình cờ "hút" mọi câu hỏi — đó đúng
là triệu chứng của retrieval kém, và test một câu không phân biệt được nó với retrieval tốt.
Ba câu, ba đáp án khác nhau, thì không tình cờ được.

Tiếng Việt thật, văn bán hàng thật (phí ship / đổi trả / giờ mở cửa) — không phải lorem ipsum
và không phải tiếng Anh. e5 là model multilingual; đo bằng tiếng Anh là đo một thứ khác với
thứ đang bán.

### [x] R3 — Skip SẠCH khi thiếu key (không FAIL giả)

OBSERVED:
```
$ env -u TOGETHER_API_KEY pytest tests/test_wiki_rag_live.py -m live -q
ssss                                                                     [100%]
```
`s` = skip, không phải `F`. Quan trọng vì CI không có key: nếu thiếu key làm test ĐỎ, người ta
sẽ tắt test thay vì sửa — và live gate chết âm thầm.

### [x] R4 — Nối hai đầu E0 ↔ E1

OBSERVED: `test_live_embedding_dim_matches_db_column` PASS — vector e5 THẬT dài đúng
`EMBED_DIM` = 1024, tức ingest được vào cột E1 vừa đổi. E0 đo dim bằng gọi thật, E1 đổi cột,
E2 chứng minh hai đầu khớp trên đường ống thật (`ingest_wiki` → `search_wiki`), không phải
bằng hai phép đo rời.

### [x] R0 — Có gì bất ngờ không?

```
1. KHÔNG có bất ngờ kỹ thuật. Đó chính là điều đáng ngờ.
```
3/3 xếp hạng đúng ngay lần đầu, không phải tinh chỉnh gì. Lý do: **corpus này quá dễ** — 3 đoạn
chủ đề tách bạch hoàn toàn (phí ship / đổi trả / giờ mở cửa), không chồng lấn từ vựng. Retrieval
gần như không thể sai. Đừng đọc kết quả này thành "chất lượng retrieval đã nghiệm thu".

```
2. Cái E2 KHÔNG chứng minh
```
- Corpus thật của seller (PRE-003 chưa land): hàng trăm chunk, chủ đề chồng lấn, văn phong thật.
  Phải đo LẠI khi có.
- Chưa có index vector (ivfflat/hnsw). Với 3 row seq-scan đúng; corpus thật thì thành vấn đề
  hiệu năng. Chưa ai nhận.
- Latency 0.32–0.36s/query là trên 3 row. Không suy ra được gì cho quy mô thật.

```
3. Corpus test bị `drop_all` mỗi lần chạy
```
Live test dùng `Base.metadata.drop_all` — chạy nó trên DB có dữ liệu thật là mất sạch. Hôm nay
vô hại (dev DB), nhưng đây là live test chạy TAY, và ngày nào ai đó trỏ `DATABASE_URL` vào chỗ
khác thì không có gì chặn. Chưa vá — ghi ra để không ai bất ngờ.

---
SMOKED_BY: Claude (Opus 4.8), chạy tay 2026-07-19, key thật + Together e5 + Postgres thật
VERDICT: PASS
