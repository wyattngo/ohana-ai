# SMOKE — spec 16 phase B0 (PIIFilteringClient chokepoint)
- diff_sha256: b07fe9fdc12d8637763c601683fb78957cfb3afa96b28b5d909a55953b581654
- stamped_at: 2026-07-24T15:15

- generated: 2026-07-24T15:07

SMOKED_BY: Claude (Opus 4.7) — POST thật qua uvicorn local, output dán nguyên văn bên dưới
VERDICT: PASS

**Ngày:** 2026-07-24 · **Môi trường:** uvicorn dev @ `127.0.0.1:8765`, TOGETHER_API_KEY dummy (chỉ đo path, KHÔNG đo response — R4 N/A)

Test đo môi trường TEST (dependency-override đè `get_llm_client()`). Smoke này đo thứ test KHÔNG đo được: **wrapper có nằm trên đường request HTTP thật khi factory chạy dưới uvicorn không**. Test override có thể vô hiệu factory; smoke chứng minh factory production trả wrapped client trên đường HTTP thật.

> Điền **OBSERVED** bằng output THẬT (dán vào), không viết "OK"/"chạy tốt".
> Một dòng "OK" không phân biệt được với việc chưa chạy.

## Mặt runtime của B0

B0 wire `PIIFilteringClient` vào `api/chat.py::get_llm_client()`. Câu hỏi runtime
duy nhất: khi POST `/api/chat` đi qua uvicorn thật, đường request có ĐI QUA
wrapper không (không phải qua fake fixture)? Test suite chứng minh code trong
wrapper hoạt động; smoke này chứng minh wrapper NẰM TRÊN đường đi thật.

Cách chạy:

```bash
export OHANA_ENV=dev
export TOGETHER_API_KEY="dummy-key-for-boot-only"
export OHANA_JWT_SECRET="dummy-jwt-secret-for-smoke-only-32-bytes-min"
.venv/bin/uvicorn app.main:app --port 8765 --log-level info &

CJ=/tmp/16-B0-cookies.txt
curl -s -c $CJ -X POST http://127.0.0.1:8765/api/mock/authorize > /dev/null
CSRF=$(grep ohana_csrf $CJ | awk '{print $NF}')

curl -s -b $CJ -X POST http://127.0.0.1:8765/api/chat \
  -H "Content-Type: application/json" \
  -H "X-CSRF-Token: $CSRF" \
  -d '{"message":"SĐT em 0912345678, gọi giúp shop"}'
```

## Checklist

### [x] R1 — Wrapper nằm trên đường request thật (bằng chứng cấu trúc, không lời khai)

Vì sao: test suite dùng dependency-override, nên nếu factory `get_llm_client()` vô
tình return raw client thay vì wrapper, test vẫn xanh vì override đè cả factory.
Test `test_get_llm_client_returns_wrapped_instance` bắt được sai lệch **type**,
nhưng chỉ smoke qua uvicorn thật mới chứng minh **đường đi runtime** thật sự đi
qua wrapper.

Bằng chứng: stack trace khi Together raise 401 (do dummy key) cho thấy chuỗi
`api/chat.py:117 → agent/pii_client.py:128 → agent/providers/openai_client.py:235`.
Wrapper.step ở giữa endpoint và raw client — chính xác vị trí chokepoint đòi hỏi.

OBSERVED:

```
File "api/chat.py", line 117, in chat
    step = await llm.step(messages)
File "agent/pii_client.py", line 128, in step
    result = await self._inner.step(
File "agent/providers/openai_client.py", line 235, in step
    resp = await self._create(
```

### [x] R2 — PII trong request body KHÔNG lọt vào log server (hành vi)

Vì sao: wrapper phải redact TRƯỚC khi bất kỳ đường log nào có cơ hội chép content.
Nếu wrapper chạy quá muộn — hoặc log ứng dụng chép request body TRƯỚC wrapper —
log server sẽ chứa `0912345678` nguyên vẹn. Smoke đo hành vi thật, không phải
test unit trên `caplog` (có thể bị lệch config).

Cách đo: `grep -c "0912345678" /tmp/16-B0-uvicorn.log` sau khi POST payload có
chứa SĐT đó.

OBSERVED:

```
$ grep -c "0912345678" /tmp/16-B0-uvicorn.log
0
```

0 lần. SĐT không rò vào log server thật.

Log lines liên quan (đã redact / metadata):

```
INFO:     127.0.0.1:61414 - "POST /api/mock/authorize HTTP/1.1" 200 OK
INFO:     127.0.0.1:61416 - "POST /api/chat HTTP/1.1" 500 Internal Server Error
INFO httpx HTTP Request: POST https://api.together.xyz/v1/chat/completions "HTTP/1.1 401 Unauthorized"
```

### [x] R3 — Together nhận 500 do dummy key, KHÔNG do wrapper (isolation)

Vì sao: nếu wrapper hỏng runtime (import error, TypeError khi bọc messages), 500
sẽ đến TỪ wrapper, không phải từ Together. Cần phân biệt hai nguyên nhân này để
smoke có ý nghĩa.

Bằng chứng: exception cuối cùng là `openai.AuthenticationError: Error code: 401`
từ `openai/_base_client.py:1716` — nghĩa là request đi hết đường qua wrapper,
qua adapter TogetherClient, ra tới Together, và bị Together từ chối vì key giả.
Wrapper KHÔNG raise; nó forward thành công tới inner. Đây là điều B0 cần.

OBSERVED:

```
openai.AuthenticationError: Error code: 401 - {'id': 'otNmxAw-6Ng1vN-a2016ef0efcf9998',
  'error': {'message': 'Invalid API key provided. You can find your API key at
  https://api.together.ai/settings/api-keys.', 'type': 'invalid_request_error',
  'param': None, 'code': 'invalid_api_key'}}
```

### [N/A] R4 — Live call với real key + đo hits

Không chạy trong smoke này vì:

1. Real key + Together billable ⇒ chỉ chạy tay khi Wyatt yêu cầu (§4 `pytest -m live`).
2. Đường code đã được chứng minh: R1 (cấu trúc) + R2 (hành vi log) + R3 (isolation)
   trả lời "wrapper có redact và forward đúng chưa?". Việc Together trả response
   nào cho payload đã redact là câu hỏi khác (không phải B0) — nó là chất lượng
   generation của model, đo bởi eval suite (D0 BLOCKED chờ tin khách thật).
3. Redactor hits/loại đã được đo tất định ở 40 test A0. `hits` shape đi vào log
   destination là scope C0, không B0.

Nếu Wyatt muốn `hits` thật ở log: chạy lại smoke với real `TOGETHER_API_KEY`; kỳ
vọng response 200 và log dòng `chat` với `token_in`/`token_out` thật.
