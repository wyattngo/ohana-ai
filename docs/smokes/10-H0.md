# SMOKE — spec 10 phase H0 (schema `messages` + composite FK)
- diff_sha256: 31d5d34fc39c0dceeff08665cc9e6bebaaff7430504b142ae65821af48e54ed6
- stamped_at: 2026-07-20T12:24

SMOKED_BY: Claude (Opus 4.8) — lệnh chạy thật trên máy Wyatt, output dán nguyên văn bên dưới
VERDICT: PASS

**Ngày:** 2026-07-20 · **Môi trường:** Postgres THẬT (`drnickv4-db-1`, `pgvector/pgvector:pg16`, PG 16.14) @ `127.0.0.1:5432/ohana`

Test đo môi trường TEST (`fresh_db` dựng schema bằng `Base.metadata.create_all`, **không** qua Alembic — xem docstring `conftest.py`). Smoke này đo thứ test KHÔNG đo được: **chuỗi migration thật chạy trên DB thật**. Model đúng mà `0006` viết sai thì suite pytest vẫn xanh — nên hai thứ này không thay được nhau.

---

## 1. Chuỗi migration từ schema trống → head → down → up

```
$ .venv/bin/alembic upgrade head
[0004] xoá 0 row khỏi bảng embeddings — KHÔNG khôi phục được
$ .venv/bin/alembic downgrade -1        # 0006 → 0005
$ .venv/bin/alembic upgrade head        # 0005 → 0006
```

**OBSERVED:** cả ba lệnh exit 0, không lỗi. `downgrade -1` gỡ sạch 2 cột + 2 FK + 1 index; `upgrade` dựng lại được. Chain reversible về **schema**.

⚠️ Không reversible về **dữ liệu** — `downgrade` drop cột nên mất liên kết conversation của mọi message đã ghi. Lúc smoke này chạy bảng có 0 row nên vô hại. Ghi ra đây vì docstring `0006` cảnh báo đúng điều đó và không được đọc lướt thành "giống 0005".

## 2. Schema thật sau migration (đọc từ Postgres, không từ model)

```
$ python -c "... psycopg introspection ..."
alembic head: 0006
cột messages: ['id', 'shop_id', 'role', 'content', 'created_at', 'conversation_id', 'customer_id']
FK : ['fk_messages_conversation_same_shop', 'fk_messages_customer_same_shop']
idx: ['idx_msg_shop_conv_created', 'idx_msg_shop_created', 'messages_pkey']
messages rows: 0
```

**OBSERVED:** 7 cột (5 cũ + 2 mới), đúng 2 FK composite, **cả hai index cùng tồn tại** — `idx_msg_shop_created` (cũ, theo shop) không bị thay thế bởi `idx_msg_shop_conv_created` (mới, đường đọc history H2).

## 3. Gate

```
$ pytest tests/ -m 'not live'    → 142 passed, 6 deselected
$ mypy app agent retrieval parsing storage db bridge tools → Success: 41 files
$ ruff check . --no-cache        → All checks passed!
$ ruff format --check . --no-cache → 79 files already formatted
```

RED trước impl đã xác nhận: 5/5 test mới đỏ vì thiếu cột/FK/index (không phải lỗi import), sau impl xanh 5/5.

---

## 4. Hai sự cố trong lúc smoke — ghi lại vì cả hai sẽ lặp

**(a) pytest làm bẩn schema alembic.** `fresh_db` chạy `drop_all`/`create_all` từ `Base.metadata` trên **cùng DB** mà alembic quản lý, nhưng không đụng bảng `alembic_version`. Hậu quả: chạy pytest xong thì schema là của model còn `alembic_version` là số cũ ⇒ `alembic upgrade head` chết bằng `DuplicateColumn` trên chính cột mình vừa thêm. Không phải lỗi của `0006`.
⇒ Muốn verify chuỗi migration thì phải reset schema trước, và **đừng đọc lỗi đó thành "migration sai"**.

**(b) `drop schema public cascade` xoá luôn extension `vector`.** User `ohana` không phải superuser nên không `CREATE EXTENSION` lại được, mọi lệnh alembic sau đó chết ở `0001`. Khôi phục bằng superuser của container:

```
$ docker exec drnickv4-db-1 psql -U drnick -d ohana -c "CREATE EXTENSION IF NOT EXISTS vector"
```

Superuser là **`drnick`**, KHÔNG phải `postgres` (role `postgres` không tồn tại trong container này — `POSTGRES_USER=drnick`).

## 5. Chưa đo (cố ý)

Đường ghi/đọc message **chưa tồn tại** ở H0 — `MessageRepo`, ghi ở webhook, load last-N là H1/H2. Smoke này chỉ chứng minh **schema chịu được dữ liệu đúng và từ chối dữ liệu cross-tenant**, không chứng minh có ai ghi vào đó. Bảng vẫn 0 row sau smoke, đúng như kỳ vọng.
