# SMOKE — phase E1 (ohana-ai)

- generated: 2026-07-19T22:55
- diff_sha256: 3dab502233818e795f0fe5fac2e359f57c62c721a4837ea288c6e3e3555c0837
- stamped_at: 2026-07-19T23:09

> Điền **OBSERVED** bằng output THẬT (dán vào), không viết "OK"/"chạy tốt".
> Một dòng "OK" không phân biệt được với việc chưa chạy.

## Mặt runtime của E1

**Migration destructive trên Postgres thật.** Đây là loại thay đổi mà test không thay smoke
được: `tests/test_embedding_dim.py` chạy `alembic` qua subprocess nên nó CÓ chạm DB thật —
nhưng nó khẳng định về `atttypmod`, không cho người đọc thấy chuyện gì xảy ra với dữ liệu.
Smoke này ghi lại đúng thứ một người vận hành cần thấy trước khi chạy lệnh này ở nơi khác.

## Checklist

### [x] R1 — Migration chạy thật, và NÓI RA nó xoá bao nhiêu

OBSERVED (output nguyên văn `alembic`):
```
########## trạng thái TRƯỚC ##########
0004 (head)

########## downgrade 0004 -> 0003 ##########
[0004] xoá 2 row khỏi bảng embeddings — KHÔNG khôi phục được

########## upgrade 0003 -> 0004 ##########
[0004] xoá 0 row khỏi bảng embeddings — KHÔNG khôi phục được
```

Hai điều đáng ghi:

1. **Dòng log hoạt động đúng mục đích thiết kế.** Con số `2` không phải 2 row test fixture ban
   đầu (chúng đã mất từ lần migrate trước) — đó là 2 row do **test suite vừa chạy để lại**. Tức
   nếu ai chạy `downgrade` ở nơi có dữ liệu, họ THẤY con số trước khi mất, không phải suy ra.
   Đây chính là ca mà dòng log này tồn tại để phục vụ.
2. **`downgrade` xoá 2, `upgrade` lại xoá 0** — chứng minh bằng số rằng down KHÔNG khôi phục
   dữ liệu. Schema quay về được (kiểm ở R2), dữ liệu thì không. Một `downgrade` trông đối xứng
   là thứ khiến người ta yên tâm rollback rồi mới phát hiện dữ liệu đã đi.

### [x] R2 — Schema sau migrate, hỏi thẳng Postgres

OBSERVED (`\d+ embeddings`, qua `pg_attribute` / `pg_indexes`):
```
  id           bigint               NOT NULL
  shop_id      text                 NOT NULL
  namespace    text                 NOT NULL
  source_ref   text
  chunk        text                 NOT NULL
  embedding    vector(1024)         ← đổi từ vector(1536)
  created_at   timestamp with time zone NOT NULL
  Indexes:
    embeddings_pkey: USING btree (id)
    idx_emb_shop_ns: USING btree (shop_id, namespace)
  alembic_version: 0004   rows: 0
```

Cột đúng `vector(1024)`. **Index sống sót qua `ALTER TYPE`** — `idx_emb_shop_ns` vẫn còn; đây
là thứ tôi không assert trong test và chỉ thấy được khi nhìn schema thật. (Index đó trên
`(shop_id, namespace)`, không trên cột vector, nên `ALTER TYPE` không đụng — nhưng "suy ra là
không đụng" khác với "nhìn thấy nó còn đó".)

⚠️ **Chưa có index vector nào** (ivfflat/hnsw). Không phải hồi quy — trước E1 cũng không có.
Ghi ra vì với 2 row thì seq-scan là đúng, còn khi PRE-003 land corpus thật thì thiếu index
vector sẽ thành vấn đề hiệu năng. Không thuộc scope E1.

### [x] R3 — Ràng buộc chiều có THẬT sự chặn không (kiểm ở tầng DB, không qua ORM)

OBSERVED (insert thẳng bằng SQL, không qua model):
```
  insert vector 1024 (ĐÚNG chiều)      : CHẤP NHẬN
  insert vector 1536 (SAI chiều, cũ)   : TỪ CHỐI — (psycopg.errors.DataException)
                                          expected 1024 dimensions, not 1536
```

Đây là bằng chứng cho tuyên bố trung tâm của E1: **dim mismatch hỏng ỒN ÀO, không âm thầm.**
Khác hẳn prefix bất đối xứng ở E0 — cái đó không có lỗi nào cả. Vì Postgres từ chối ở tầng
ràng buộc, nhánh `OpenAIEmbedder` (1536) được giữ lại trong `default_embedder()` mà không nguy
hiểm: dùng nhầm thì insert nổ, không ghi được vector vô nghĩa nào vào corpus.

### [–] R4 — Rò tenant
**N/A** — E1 không chạm đường tenant. `ALTER TYPE` trên một cột; scope `shop_id` ở
`PgvectorRetriever` không đổi. `tests/test_tenant_isolation.py` có sửa nhưng chỉ là `VECTOR_DIM`
đọc từ `EMBED_DIM` thay vì hardcode — assertion isolation giữ nguyên và vẫn xanh.

### [x] R0 — Có gì bất ngờ không?

```
1. `downgrade` xoá 2 row mà tôi tưởng bảng đang trống
```
Ở bước 7 tôi kiểm và thấy `rows: 0`. Đến bước 9 thì `downgrade` báo xoá 2. Không phải lỗi — test
suite chạy giữa hai thời điểm và để lại row. Nhưng nó cho thấy **"tôi vừa kiểm thấy trống" không
phải bằng chứng cho "lúc chạy migration nó vẫn trống"**. Với migration destructive, khoảng cách
giữa lúc kiểm và lúc chạy chính là chỗ dữ liệu chui vào. Đó là lý do con số phải được in ra tại
thời điểm chạy, chứ không kiểm trước rồi tin.

```
2. Ruff S608 báo nhầm câu print, và BỎ QUA f-string dựng SQL thật ở ngay dưới
```
`print(f"...DELETE FROM embeddings...")` bị bắt là SQL injection; còn
`op.execute(f"alter table ... vector({target_dim})")` — f-string thật sự dựng SQL — thì không.
Dòng sau an toàn (`target_dim` là hằng số module, không có đường cho input ngoài), nhưng bài học
là: **im lặng của linter không phải lời bảo đảm.** Đã ghi comment tại chỗ.

```
3. Không có gì bất ngờ về e5 — E1 không gọi mạng
```
Toàn bộ smoke này là DB-local. Việc e5 sinh vector 1024 dùng được thật đã chứng minh ở
`docs/smokes/08-E0.md` R1. Nối hai đầu (ingest corpus thật → search trả đúng chunk, trên e5) là
việc của E2, chưa làm.

---
SMOKED_BY: Claude (Opus 4.8), chạy tay 2026-07-19, Postgres thật tại DATABASE_URL của `.env`
VERDICT: PASS
