# SMOKE — phase E0 (ohana-ai)

- generated: 2026-07-19T22:11
- diff_sha256: 93201661e38955ea266a1b0e8fb7da2f3cd9a79e2f1c5e6dbcf194a417e8b3a0
- stamped_at: 2026-07-19T22:23

> Điền **OBSERVED** bằng output THẬT (dán vào), không viết "OK"/"chạy tốt".
> Một dòng "OK" không phân biệt được với việc chưa chạy.

## Mặt runtime của E0

Scaffold liệt kê mọi file trong diff; mặt runtime THẬT của E0 chỉ có một:
**gọi Together/e5 qua mạng**. E0 không thêm endpoint, không thêm log, không chạm
đường tenant — nên R2/R3/R4 dưới đây là N/A có lý do, không phải bỏ qua.

Cách chạy: nạp `.env` vào một tiến trình con (Settings cố ý không đọc `.env`), gọi
`TogetherEmbedder` thật. Script nằm ở scratchpad, **không commit** — nó đọc `.env`;
để trong repo là mời người sau chạy nhầm với key thật.

## Checklist

### [x] R1 — Gọi provider ngoài THẬT (không fake)
Vì sao: cả 12 test của E0 dùng fake client. Chúng chứng minh "adapter gắn prefix đúng
bên", KHÔNG chứng minh "Together chấp nhận model id này và trả đúng số chiều". Đúng
khoảng trống đã cho ra 2 lỗi ở spec 07.

OBSERVED:
```
base_url        : https://api.together.xyz/v1
TOGETHER_EMBED_MODEL (.env) : 'intfloat/multilingual-e5-large-instruct'
key             : SET, 50 ký tự (không in)

model gửi đi    : 'intfloat/multilingual-e5-large-instruct'

HTTP            : 200 (không raise)
dim query       : 1024
dim passage     : [1024, 1024]
latency cold    : 1.17s
latency warm    : 0.35s
```

Ba thứ chốt được từ đây:
1. Model id **được chấp nhận thật** — không phải suy từ `/v1/models` (spec 07 đã dạy
   rằng có trong danh sách kèm bảng giá vẫn có thể 400 "non-serverless").
2. **dim thật = 1024** ⇒ `Vector(1024)` của migration E1 có cơ sở đo, không phải đọc doc.
3. `.env` đang trỏ đúng e5 — trước smoke tôi chỉ biết nó dài 39 ký tự.

### [x] R1b — Prefix có thật sự đổi kết quả không (đối chứng 3 chiều)
Vì sao: gate bất đối xứng trong test chỉ kiểm *chuỗi gửi đi*. Nó không trả lời được
"prefix có đáng không" — chỉ model thật trả lời được.

Mẫu: query `"phí ship bao nhiêu"` · chunk ĐÚNG = câu về phí vận chuyển · chunk SAI =
câu về lịch nghỉ Tết.

OBSERVED:
```
[CÓ prefix]    cos(q, ĐÚNG) = +0.8785   cos(q, SAI) = +0.7807   biên = +0.0978
[KHÔNG prefix] cos(q, ĐÚNG) = +0.8802   cos(q, SAI) = +0.8021   biên = +0.0780
[HOÁN ĐỔI]     cos(q, ĐÚNG) = +0.8668   cos(q, SAI) = +0.7885   biên = +0.0783

xếp hạng ĐÚNG (có prefix)? True
```

Đọc số cho đúng, **không tô hồng**:
- Prefix đúng bên cho biên rộng nhất: **+0.0978** vs +0.0780 (không prefix) và +0.0783
  (hoán đổi). Prefix có tác dụng đo được, ~+25% tương đối so với không prefix.
- **Nhưng hoán đổi KHÔNG làm đảo thứ hạng trên mẫu này** — chunk đúng vẫn thắng. Nghĩa
  là ca hỏng do prefix là **suy giảm dần**, không phải vỡ dứt khoát. Trên cặp dễ như mẫu
  này nó vẫn ra kết quả đúng; chỗ nó cắn là những cặp sát nhau — đúng loại truy vấn khó
  mà seller gặp thật.
- Kết luận thành thật: smoke này chứng minh prefix **giúp**, và gate bất đối xứng đang
  canh một thứ có thật. Nó **không** chứng minh hoán đổi prefix sẽ gây trả lời sai —
  muốn khẳng định điều đó cần E2 (live acceptance, nhiều truy vấn, assert thứ hạng).

### [–] R2 — Endpoint trên SERVER THẬT
**N/A** — E0 không thêm/đổi endpoint nào. `api/admin.py` có sửa nhưng chỉ là kiểu
(`_EmbedderProto`) + đổi base class của `_DeterministicDevEmbedder`; không đổi route,
không đổi request/response. Mặt HTTP của embedder swap nằm ở E1 (`default_embedder()`
wire vào factory) và sẽ smoke ở đó.

### [–] R3 — Log quan sát được trong log server
**N/A** — E0 không thêm dòng log nào. (Adapter cố ý không log: text đi vào embedder là
nội dung khách; log ở tầng này là mở một đường rò PII không ai rà.)

### [–] R4 — Rò tenant: gửi `shop_id` giả trong body
**N/A** — E0 không chạm đường tenant. `TogetherEmbedder` không nhận `shop_id`, không
chạm DB; scope tenant nằm ở `PgvectorRetriever(shop_scope=)`, E0 không sửa file đó.

### [x] R0 — Có gì bất ngờ không?

OBSERVED — bốn thứ:

```
1. latency cold = 1.17s, warm = 0.35s
```
CLAUDE.md §4 ghi "Cold start ~24.8s, call sau ~1.2s ⇒ UI bắt buộc có loading state".
Con số đó là của **chat model** (Llama-70B). Endpoint embedding **không** mang cold
start đó — 1.17s ngay lần gọi đầu. Đừng mang giả định 24.8s sang thiết kế UI của ingest;
ngược lại cũng đừng bỏ loading state ở chat vì thấy số này.

```
2. Biên đo được (+0.0978 / +0.0780) KHÁC số trong spec §5.5 (+0.1249 / +0.1101)
```
Khác mẫu văn bản nên **không so sánh trực tiếp được** — tôi dùng cặp câu khác. Hướng thì
tái lập: có prefix > không prefix. Ghi ra để người sau đọc spec §5.5 rồi chạy lại không
tưởng là hồi quy.

```
3. repr rò key? False
```
**Sửa lời khai của chính tôi** (reviewer bắt được, finding đúng): bản đầu artifact này viết
"assertion (g) có bản đối chứng thật". Sai. `TogetherEmbedder` không lưu key làm attribute
và không có `__repr__` custom ⇒ `object.__repr__` vốn đã không in key; chạy thật chỉ xác
nhận một trạng thái vốn đã đúng vì không có gì để rò. Nó **không** chứng minh có lớp bảo vệ
nào đang hoạt động. Giá trị thật của (g) là gate TƯƠNG LAI — ngày ai đó thêm `__repr__` hoặc
gán `self._api_key`, nó đỏ.

```
4. TOGETHER_MODEL= trong .env đang RỖNG
```
Không ảnh hưởng E0 (đó là chat model). Ghi vì nó đúng là dòng đã gây 404 ở spec 07 và
hiện vẫn còn nguyên trong `.env` của máy này. `_blank_env_means_unset` phủ được field
`str` nên nó rơi về default đúng — tức hệ thống đang **dựa vào** lớp phòng thủ đó chứ
không phải đang sạch.

---
SMOKED_BY: Claude (Opus 4.8), chạy tay 2026-07-19, key thật từ `.env`, tiến trình con riêng
VERDICT: PASS
