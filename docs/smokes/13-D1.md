# SMOKE — phase D1 (ohana-ai)

- generated: 2026-07-21T15:28
- diff_sha256: a3417442de0c97e9777c208e0d69cef7b6b7356f104c7d33a7afe9634ce14c6d
- stamped_at: 2026-07-21T15:29

> OBSERVED = output THẬT của `pytest tests/test_drafter_live.py -m live` trên Together (Llama-3.3-70B)
> qua tool-loop grounding THẬT (`lookup_size` + Postgres). Fake LLM KHÔNG chứng minh được model
> có thật sự gọi tool để tra fact hay chỉ đoán.

## Runtime exercised
- `LLMDrafter(TogetherClient(), sf, tools=[build_size_tool(sf)]).draft(...)` — real provider +
  real tool-loop + real Postgres. Shop seed bảng size token đặc biệt `XL3` cho 160cm/50kg (size
  model KHÔNG bao giờ tự đoán ra ⇒ draft chứa XL3 = nó ĐÃ tra tool).
- Message: "Mình cao 160cm nặng 50kg thì shop tư vấn mặc size nào ạ?"

## R1 — Grounding THẬT qua tool-loop (không fake)
Lệnh:
```
TOGETHER_API_KEY=<real> .venv/bin/python -m pytest \
  tests/test_drafter_live.py::test_drafter_grounds_size_from_tool_not_guess -m live -q -s
```
OBSERVED (sau khi vá — xem R0):
```
[SMOKE 13-D1] intent=general confidence=0.7
[SMOKE 13-D1] text='Chào bạn, shop tư vấn size XL3 cho bạn nhé.'
.
1 passed
```
Kết luận R1:
- Draft chứa **`XL3`** = size seed trong bảng shop ⇒ model **ĐÃ gọi `lookup_size`** (grounded),
  KHÔNG đoán. Loop dispatch `shop_id` từ param, xâu tool result, model đọc rồi trả lời đúng.
- `intent=general` ∈ enum, `confidence=0.7` ∈ [0,1]. Không lộ danh tính.

## R0 — Có gì bất ngờ không?
OBSERVED:
```
CÓ — 2 phát hiện, mỗi cái một RETRY, đều do smoke bắt (fake LLM không thấy):

RETRY 1: offer grounding tools CÙNG emit_reply ⇒ Llama-3.3 tra tool xong TRẢ LỜI BẰNG content
thay vì gọi emit_reply, loop raise cứng. Fix (in-scope agent/drafter.py): _finalize — model trả
content mà CÓ grounding tools ⇒ 1 lượt cuối chỉ offer emit_reply (D0 chứng minh model gọi tin
cậy khi đó là tool duy nhất). Shape D0 (no tools) giữ ngữ nghĩa content→raise.

RETRY 2: model ĐOÁN size 'S' thay vì gọi lookup_size — draft đầu 'mặc size S' (sai; seed là
XL3). Nguyên nhân: persona prompt không nhắc tool nên model không biết PHẢI tra. Fix (in-scope):
_GROUNDING_DIRECTIVE chèn vào system prompt khi có grounding tool — 'fact phải tra tool, cấm
đoán, not_found không bịa'. Re-smoke → draft chứa XL3.

Ghi chú kiến trúc (không chặn D1): grounding hiện là phòng thủ MỀM tầng prompt. Hàng rào cứng
(tool_choice="required" ở provider / eval bắt câu ungrounded) là GD0-EVAL / GD0-INTENT về sau.
```

---
SMOKED_BY: Claude (AI-coder, main loop) · key thật từ .env · 2026-07-21
VERDICT: PASS
