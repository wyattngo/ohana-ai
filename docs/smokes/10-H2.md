# SMOKE — spec 10 phase H2 (read path: last-N vào `Drafter` + cap kép)
- diff_sha256: 5ea5bc2f8ba9061b80523b005e5123dacdfe26edcd43f556f8abe10670be4c58
- stamped_at: 2026-07-20T20:24

SMOKED_BY: Claude (Opus 4.8) — script chạy thật trên Postgres thật, output dán nguyên văn
VERDICT: PASS

**Ngày:** 2026-07-20 · **Môi trường:** Postgres THẬT (`drnickv4-db-1`, pgvector pg16) @ `127.0.0.1:5432/ohana`

Test đo `fresh_db` (schema từ `create_all`). Smoke này đo cái test không đo: **luồng thật lượt-2 trên DB thật**, gồm cả câu hỏi "shop khác đọc được không".

---

## 1. Lượt 2 — history có tới drafter không

Seed 2 message ở lượt 1, rồi gọi `receive_and_draft` với câu lượt 2 dùng đại từ.

```
CAP: 20 message / 4000 ký tự
HISTORY drafter nhận:
   user       | áo thun trắng bao nhiêu
   assistant  | dạ 250k ạ
OUTCOME: park | park:low_confidence
shop KHÁC đọc cùng conversation: []
```

**OBSERVED:**
- Drafter nhận đủ 2 message, **thứ tự cũ→mới** (`user` trước `assistant`). Đây là điều kiện để "cái áo đó" phân giải được.
- `MessageRepo(shop_scope="shop_khac").last_n(conv)` trả **`[]`** — rỗng, không raise. Raise sẽ phân biệt được "không tồn tại" với "tồn tại nhưng của shop khác", tức rò rỉ chính sự tồn tại.
- Outcome `park:low_confidence` — đường gửi khách KHÔNG bị H2 đụng tới.

## 2. Gate

```
pytest tests/ -m 'not live'  → 154 passed, 6 deselected   (H1: 148 → +6)
mypy …                        → Success: 41 source files
ruff check . --no-cache       → All checks passed!
ruff format --check           → 79 files already formatted
```

RED trước impl: `ImportError: cannot import name 'HISTORY_MAX_CHARS'` — API chưa tồn tại, cùng dạng RED của H0.

## 3. Hai thứ smoke này KHÔNG chứng minh

**(a) Không chứng minh AI trả lời đúng.** Drafter là spy tất định. "History tới được drafter" và "LLM dùng được history" là hai mệnh đề khác nhau; mệnh đề sau cần `-m live` + eval, và một test phụ thuộc chất lượng LLM sẽ đỏ ngẫu nhiên rồi bị tắt.

**(b) Không chứng minh history ĐẦY ĐỦ.** Nó đầy đủ với `auto_send`, KHÔNG đầy đủ với `park` (PRE-1004): reply seller duyệt chưa vào `messages`. Mà `park` là đường mặc định — shop chưa opt-in intent nào thì mọi reply đều qua đó. Nên trên thực tế hôm nay, history = tin khách + reply auto-send, **thiếu toàn bộ reply seller duyệt**.

## 4. Phát hiện trong lúc làm — CHƯA VÁ, ngoài ALLOWED_FILES

**`api/webhook.py:33` khai một Protocol đã sai.**

```python
class _Drafter(Protocol):
    async def draft(self, *, shop_id: str, customer_id: str, message: str): ...  # type: ignore[no-untyped-def]
```

`agent.orchestrator.Drafter` giờ đòi thêm `history`, nhưng bản sao trong webhook thì không. mypy **không bắt được** vì `# type: ignore[no-untyped-def]` làm nó bỏ qua so khớp. Hệ quả: ai viết `Drafter` thật dựa theo Protocol của webhook sẽ thoả type-check rồi **nổ `TypeError` lúc chạy** khi orchestrator gọi kèm `history=`.

Chưa vá vì `api/webhook.py` không nằm trong `ALLOWED_FILES` của H2. Đây là bản sao Protocol thứ hai của cùng một khái niệm — nguyên nhân gốc là sự trùng lặp, không phải một dòng sai.
