# macOS
.DS_Store
._*

# Python (Phase 1.1+ sẽ dùng)
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
# .python-version — INTENTIONALLY tracked (Phase 1.1 pin — pyenv/rye auto-select 3.11)

# IDE
.vscode/
.idea/

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key
secure/

# ADP generated artifacts (regeneratable từ spine)
docs/adp-dashboard.html
docs/adp-progress-dashboard.html
.adp-audit.jsonl
.adp-state/

# Claude local overrides (không share)
.claude/settings.local.json

# Logs
*.log
logs/

# Test cache
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage

# Lịch sử mẫu số roadmap (DEC-OHANA-03 §0.2) — là BẰNG CHỨNG chống gian lận
# chỉ số, phải vào git; đừng để rule `*.log` ở trên nuốt mất.
!docs/.roadmap-denominator.log
